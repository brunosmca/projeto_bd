<!--> <p id="notice"><%= notice %></p> <-->
<h1>Projetos</h1>

<p>
  <b>Nome:</b>
  <%= @project.nome %>
</p>

<p>
  <b>Data inicial:</b>
  <%= @project.data_inicial %>
</p>

<p>
  <b>Data final:</b>
  <%= @project.data_final %>
</p>

<table>
  <tr>
    <th>Titulo</th>
    <th>Data inicial</th>
    <th>Date prev final</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @project.activities.each do |activity| %>
  <tr>
    <td><%= activity.titulo %></td>
    <td><%= activity.data_inicial %></td>
    <td><%= activity.date_prev_final %></td>
    <td><%= link_to 'Show', activity %></td>
    <td><%= link_to 'Edit', edit_activity_path(activity) %></td>
    <td><%= link_to 'Destroy', activity, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<p> 
	<%= link_to 'Nova Atividade', new_activity_path %>
</p>


<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'Back', projects_path %>
